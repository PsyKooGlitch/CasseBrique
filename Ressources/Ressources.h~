#ifndef RESSOURCES_H
#define RESSOURCES_H

// Macros pour les couleurs de bille
#define GRISE                      400000
#define ROUGE                      400001
#define BLEUE                      400002
#define JAUNE                      400003
#define MAUVE                      400004
#define VERTE                      400005
#define MAGENTA                    400006

int  OuvertureFenetreGraphique();
int  FermetureFenetreGraphique();
void DessineMur(int l,int c);
void DessineBille(int couleur,int l,int c);
void DessineRaquetteBas(int l,int c);    // (l,c) coordonnees du centre de la raquette
void DessineRaquetteHaut(int l,int c);   // (l,c) coordonnees du centre de la raquette
void DessineRaquetteGauche(int l,int c); // (l,c) coordonnees du centre de la raquette
void DessineRaquetteDroite(int l,int c); // (l,c) coordonnees du centre de la raquette
void DessineVerrou(int l,int c);
void DessineChiffre(int chiffre,int l,int c);

#endif
